
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'dotnet build MyAPI.sln'
                echo "Building......."
            }
        }
    }
    
	
}
